# -------------------------------
# Production Profile (Cloudinary)
# -------------------------------
jobhunter.storage.type=CLOUDINARY

# Hide SQL in production
spring.jpa.show-sql=false

# VNPAY Return URL for Production (Vercel)
vnpay.returnUrl=${VNPAY_RETURN_URL:https://jobhunter-fe.vercel.app/hr/payment/return}

# Frontend URL
jobhunter.frontend.url=${FRONTEND_URL:https://jobhunter-fe.vercel.app}

# Cloudinary is used, so UPLOAD_DIR is not needed for storage but kept for reference
